---
interface Props {
  text: string;
  href: string;
  bdColor?: string;
  bgColor?: string;
  textColor: string;
  textSize?: string;
  target?: string;
}

const {
  text,
  href,
  textColor,
  bdColor = "primaryMed",
  bgColor = "white",
  textSize = "sm",
  target = "_self",
} = Astro.props;
---

<a
  href={href}
  target={target}
  class=`text-${textColor} bg-${bgColor} focus:outline-none cursor-pointer focus:ring-4 focus:ring-secondary font-medium rounded-full text-${textSize} px-5 py-2.5 text-center border border-${bdColor} `
  >{text}</a
>

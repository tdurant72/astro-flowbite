---
import { Image } from "astro:assets";
import Button from "../Button.astro";
import ButtonAlt from "../ButtonAlt.astro";

import siteImage from "../../../public/images/eagles-device.webp";

const title = "Let's turn your website to a customer magnet";
const body =
  "Our simple 8-point plan will increase your page speed, help you rank higher in search engines, and expand customer base, all in 90 days";
const buttonText = "Book My Free Evaluation";
const buttonLink = "/#contact-us";
const buttonTwoText = "Get Started";
const buttonTwoLink = "https://www.google.com";
---

<section
  class="bg-slate-50 dark:bg-slate-800 lg:h-full section-parallax mt-10 md:mt-4"
>
  <div
    class="grid max-w-screen-xl px-4 py-8 mx-auto lg:gap-8 xl:gap-0 lg:py-16 lg:grid-cols-12"
  >
    <div class="mr-auto place-self-center lg:col-span-7 copy-parallax">
      <h1
        class="text-primaryMed drop-shadow-[2px_2px_var(--tw-shadow-color)] shadow-gray-800 max-w-2xl mb-4 text-4xl font-extrabold tracking-tight leading-none md:text-5xl xl:text-6xl dark:text-white"
      >
        {title}
      </h1>
      <p
        class="max-w-2xl mb-6 font-light text-gray-700 lg:mb-8 text-lg md:text-xl lg:text-2xl dark:text-gray-400"
      >
        We can increase your page speed, help you rank higher in search engines,
        and expand customer base, all in 90 days
      </p>
      <p
        class="max-w-2xl mb-6 font-light text-gray-700 lg:mb-8 text-lg md:text-xl lg:text-2xl dark:text-gray-400"
      >
        We offer a <span class="text-primaryDrk underline">100% free</span>
        evaluation of your existing site.
      </p>
      <div class="flex flex-col items-start space-y-2">
        <Button
          text={buttonText}
          href={buttonLink}
          textSize="lg"
          bgColor="primary"
          textColor="primaryBlk"
        />
        <span class="text-xs mx-4">We will never share your information</span>
      </div>
      <span class="mr-4"></span>
    </div>
    <div
      id="hero-graphic"
      class="hidden lg:mt-0 lg:col-span-5 lg:flex lg:justify-center"
    >
      <!-- <img
        src="https://flowbite.s3.amazonaws.com/blocks/marketing-ui/hero/phone-mockup.png"
        alt="mockup"
      /> -->
      <Image
        src={siteImage}
        width={211}
        height={425}
        alt="Site Image"
        loading="eager"
        class="rotate-2 dark:hidden ease-in-out duration-300 hover:rotate-0 hover:scale-105"
      />
      <Image
        src={siteImage}
        width={211}
        height={425}
        alt="Site Image"
        loading="eager"
        class="image-parallax hidden dark:block ease-in-out duration-300 hover:rotate-1 hover:scale-105"
      />
    </div>
  </div>
</section>

<style>
  .section-parallax {
    transform-style: preserve-3d;
    z-index: -1;
  }
  .image-parallax {
    transform: translateZ(-16px) scale(2);
  }
  @keyframes float {
    0% {
      transform: translate3d(0, 0, 0);
    }

    100% {
      transform: translate3d(0, 30px, 0);
    }
  }

  #hero-graphic {
    animation: float ease-in-out 5.5s infinite alternate;
  }
</style>
